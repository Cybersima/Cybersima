# Reports

`trading_journal_dashboard.html` — offline dark-theme journal dashboard built from a SecureTrade audit bundle (`trade_history.csv`) plus Yahoo Finance (`yfinance`) market enrichment.

Open the HTML file directly in a browser (no server, no CDN).
