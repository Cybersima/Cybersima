CyberSym SecureTrade 1.6

This is product version 1.6.
Cursor unzip folders (v01.3, v01.4, v01.5, …) are download names, not the product version.
If start.bat prints "CyberSym SecureTrade 1.6", you have this build.

The encodings crash came from launching .venv with python -E on Python 3.14.
1.6 starts with your real Python 3.14 (the one that already runs v01.3)
and does not use -E.

Keep using Python 3.14. Do not copy .venv from Version 4.
