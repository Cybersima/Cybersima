# Customer installer

This folder holds the downloadable product zip.

- File: `CyberSym-SecureTrade-2.1.0.zip`
- Launchers inside: `start.bat` (Windows), `start.command` (macOS), `start.sh` (Linux)

Rebuild with:

```bash
python -m securetrade package
```
